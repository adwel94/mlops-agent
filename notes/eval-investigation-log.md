# 평가 조사 일지 — "왜 학습한 로봇이 큐브를 잘 못 집을까"

이 문서는 그 답을 찾아가는 **과정의 이야기**다. 무슨 문제를 만났고, 무엇을 의심했고,
어떻게 확인했고, 무엇을 알게 됐는지를 순서대로 적는다. (정확한 수치·코드 위치는 맨 끝
부록에. 요약 표는 `model-eval-diagnosis.md`. 이 진단에서 얻은 추론 규율은 CLAUDE.md 협업 규칙 "추론 규율"에 반영됨.)

---

## 한눈에 보는 결말

우리 로봇 모델은 **"빨강 큐브 집어"** 같은 지시를 받고 큐브를 집는 일을 한다. 그런데
성공률이 ~20%밖에 안 나왔다. 파고들어 보니:

- **색깔은 거의 안 틀린다** (집기만 하면 94% 맞는 색) — 언어 이해는 문제 아님.
- 진짜 문제는 **손을 큐브까지 정밀하게 못 가져간다** — 평균 8cm 빗나가서 헛손질.
- 이건 측정 오류도, 데이터 불량도 아닌 **진짜 모델 한계**임을 (공짜 검증으로) 확인.
- 원인: 모델의 **"눈"이 얼어 있어서** 큐브의 정확한 3D 위치를 못 짚는다.
- 다음 수: **눈을 녹여 다시 학습**(visual 백본 fine-tune) — NVIDIA 공식 처방과도 일치.

---

## 0. 먼저, 우리가 뭘 만들었나

테이블 위에 빨강·초록·파랑 큐브 3개가 있고, **"○○색 집어"**라고 시키면 그 색 큐브를
집어 드는 로봇 팔. 이걸 GR00T라는 거대 AI 모델로 학습시켰다.

**채점(평가) 방법**: 학습한 모델을 클라우드 GPU에 올려두고(serve), 내 PC의 시뮬레이터에서
로봇을 굴리며 모델에게 "다음에 어디로 움직일까?"를 물어본다. 모델이 답한 손 위치를
관절 각도로 바꿔(IK) 실제로 움직이고, **시킨 색 큐브를 집어 들면 성공**으로 친다.

기준점 하나: 원래 시범 데이터(사람이 만든 정답 궤적)는 이 채점에서 **100% 성공**한다.
그러니 모델이 그보다 낮으면 그만큼이 모델의 부족분이다.

---

## 1. 문제: 성공률이 20%밖에 안 나온다

학습을 길게(스텝 20,000) 시켰는데도 성공률 ~18%. 짧게(3,000) 시킨 것과 **똑같았다.**
이상한 단서가 셋이었다:

1. **학습 오차(loss)는 거의 0까지 떨어졌다** → 모델이 배울 건 다 배웠다는 뜻.
2. **로봇이 "갈 수 없는 자세"를 요청한 적은 한 번도 없다** → 동작이 미친 건 아님.
3. **더 오래 학습해도 똑같다** → "학습이 부족해서"는 아님.

정리하면 **"모델이 그럴듯하게 움직이는데 과제만 못 푼다."** 그럼 왜?

---

## 2. 의심이 바뀌어 간 과정

### 의심 1 — "데이터나 변환 과정이 잘못된 거 아냐?"

EE(손끝) 좌표 변환·정규화 같은 데 숨은 버그를 의심했다. 그런데 **새 실험 없이도** 이미
가진 증거로 반박됐다: 변환은 이미 다른 게이트에서 검증돼 있었고, 학습이 정상적으로
끝났다는 것 자체가 "데이터·정규화는 자기 일관적"이라는 뜻이었다. → **데이터 결백.**
화살은 "데이터"가 아니라 "모델/방법"으로 향했다. *(이 부분은 처음에 내가 과장했다가
사용자 반박으로 정정 — 재발 방지 규율은 CLAUDE.md 협업 규칙 "추론 규율".)*

### 의심 2 — "색깔을 못 골라서 그런 거 아냐?" → **반전**

가장 그럴듯한 가설이었다. 학습할 때 모델의 "눈"과 "언어" 부분을 얼리고 손동작만
가르쳤으니(=head만 학습), "빨강이라는 단어 → 화면의 빨간 큐브" 연결을 못 배웠을 수 있다.
색을 랜덤으로 찍는다면 3개 중 1개(33%)만 맞을 거고, 집기 성공률과 곱하면 ~18%가 나온다.
계산이 딱 맞았다.

**그래서 측정했다 — "아무 큐브나 집었나" vs "시킨 색을 집었나"를 따로 셌다.**
(방법: 시뮬레이터 안에서 큐브 3개 각각이 잡혔는지·들렸는지 직접 들여다보고, 집어 든
큐브의 색을 목표색과 대조.)

결과는 **반전**이었다:

> **집은 큐브의 94%가 정확히 목표색.** 색깔은 거의 안 틀렸다.

색을 모르면 33%여야 하는데 94%다. → **언어/색 이해는 병목이 아니다.** 진짜 문제는
따로 있었다: **아무 큐브나 집어 드는 것 자체가 34%밖에 안 됐다.** 못 집은 경우, 손은
큐브에서 평균 **8cm 떨어진 데서** 멈췄다(성공할 땐 1cm). → 병목은 **"손을 큐브까지
정밀하게 못 가져간다"**, 즉 reaching.

*(이 가설을 측정 없이 믿고 비싼 풀-학습으로 갔으면 — 언어는 멀쩡한데 언어부를 손봐서
헛돈을 쓸 뻔했다. 측정이 막았다.)*

### 의심 3 — "16스텝을 한 번에 실행해서 도중에 빗나가나?" → 반증

모델은 한 번 보고 **앞으로 16스텝**의 손동작을 예측한다. 우리는 그 16개를 통째로
실행한 뒤에야 다시 카메라를 봤다. "눈 감고 16발짝 걷는" 셈이라, 도중에 살짝 어긋나면
못 고치고 8cm 빗나가는 게 아닐까?

검증: **얼마나 자주 다시 보는지(action_steps)만 16 → 8 → 4로 줄여** 재평가(학습은 그대로).
자주 보면 좋아져야 가설이 맞다. 결과:

| 다시 보는 주기 | 아무 큐브나 집기 | 성공률 |
|---|---|---|
| 16 (통째 실행) | 34% | 32% |
| 8 | 8% | 8% |
| 4 | 2% | 2% |

**자주 볼수록 오히려 더 나빠졌다.** → 드리프트 가설 반증. 알고 보니 이 모델은 한 번에
길게 움직일수록 일관되고, 자주 다시 계획하면 **매번 살짝 다른 계획을 새로 뽑아 덜덜대다**
못 집는다(diffusion 모델 특성). 즉 **지금이 이미 최적 설정**이고, 실행 방식으로는 못 고친다.

→ 남은 결론: **병목은 모델이 손을 정밀하게 못 가져가는 것** 그 자체.

---

## 3. "이게 진짜 모델 문제 맞아?" — 돈 쓰기 전 공짜 검증 둘

비싼 재학습에 들어가기 전에, "8cm 빗나감"이 정말 모델 탓인지 **혹시 우리 측정이
잘못된 건 아닌지**부터 공짜로 확인했다.

### 검증 ① 채점이 정직한가 (다트판이 아니라 답안지 점검)

학생(모델)을 채점하는데 **내 답안지가 틀렸으면** 학생이 맞아도 틀렸다고 나온다.
모델 출력 → 실제 로봇 움직임 사이엔 변환 단계가 여럿이라, 거기 버그가 있으면 모델을
억울하게 깎는다.

**확인법(모델 없이)**: 우리에겐 **100% 성공하는 정답 시범 행동**이 있다. 그걸 평가가
쓰는 **똑같은 변환 사슬**에 넣어 시뮬레이터에 돌려봤다.

> 결과: **재현율 100% (50/50).** 정답을 넣으니 100% 성공.

→ **채점(변환·좌표·IK)은 정직하다.** 모델이 실패한 건 진짜 모델 탓이다. (덤으로 **데이터
품질도 완벽**임이 확인됐다.)

### 검증 ② 빗나감이 "한쪽으로 일정"이냐 "제멋대로"냐

다트가 **전부 오른쪽 8cm**에 꽂히면 → 가늠자(좌표)가 틀어진 버그(공짜 수정). **사방으로
흩어지면** → 그냥 손이 부정확한 것(학습 필요).

실패한 경우의 빗나간 거리를 보니 **0~13cm로 넓게 흩어져 있었다**(한 값에 안 뭉침).
→ 고정 버그가 아니라 **진짜 부정확(산포)** 쪽. *(단 거리만 봤고 방향까진 못 봐서, 다음
실행 때 방향까지 기록되게 로깅을 보강해 뒀다.)*

**검증 종합**: 채점 정직 + 데이터 완벽 + 빗나감 산포 → **8cm 빗나감은 측정 착시가 아니라
진짜 모델 한계.** 이제 학습이 정당화된다.

---

## 4. "그럼 뭘 고쳐야 하나?"

### 왜 정밀하게 못 가나 — 모델이 보는 것과 모르는 것

모델은 매 순간 **세 가지**를 입력으로 받는다:

- 📷 카메라 영상
- 🦾 **자기 손 위치**(관절·손끝 좌표) ← 센서로 *정확히* 주어짐
- 💬 언어 지시("빨강")

핵심: **자기 손이 어디 있는지는 모델이 정확히 안다.** 화면에서 추정해야 하는 건
**큐브가 3D로 어디 있는가**뿐이다(큐브 좌표는 입력에 없고 영상에만 있음).

그런데 **시범 데이터는 큐브의 *정확한 좌표*를 알고 만들어졌고**, 모델은 그 좌표를
**카메라 픽셀로 눈대중 추정**해야 한다. 그게 둘의 결정적 차이. 게다가 카메라가 **고정된
하나**라 깊이(멀고 가까움) 추정이 애매 → 큐브를 cm 단위로 잘못 짚고 → 손이 그만큼
빗나간다.

### "더 학습해도 0개선"의 의미 — 눈이 얼어 있었다

여기서 사용자가 날카로운 질문을 했다: *"인지 문제라면 더 학습하면 나아져야지, 왜 0개선?"*

답: 우리 학습은 모델의 **"눈"(시각 인코더)을 얼려놨다.** 우리가 늘린 학습은 눈은 그대로
두고 **손동작 부분만** 더 돌린 것. 눈이 얼었으니 **인지는 아무리 학습해도 안 변한다.**
그래서 평탄했던 것 = **천장이 "얼린 눈"에 있다**는 증거. 고치려면 **눈을 녹여야** 한다.

### 공식 문서는 뭐라 하나 (GR00T 가이드)

NVIDIA의 GR00T fine-tune 가이드를 찾아봤다. 핵심:

- **기본 설정 = 눈·언어 백본을 *둘 다 얼리고*, 연결층(projector) + 동작 head만 학습.**
  → 이게 **정확히 우리가 한 것**이다. (처음엔 "우리가 LLM까지 과하게 얼린 듯"이라 적었다가,
  더 신뢰할 출처로 확인하니 **기본이 원래 둘 다 얼림 = 우리는 표준을 그대로 따른 것**으로
  정정. 빠뜨린 실수는 없었다.)
- GR00T는 일부러 **비전에 의존하도록** 설계됐다(학습 때 자기 위치 정보를 가끔 가려서
  영상을 보게 만듦). 즉 "얼린 눈이 충분히 잘 본다"는 전제를 깐다.
- **그 전제가 안 통할 때의 공식 처방 = `tune_visual`(눈 녹이기) / `tune_llm`.** "compute를
  더 써서 백본을 더 학습하라"고 안내한다.

→ 우리가 데이터로 도달한 결론("얼린 눈이 천장")과 **NVIDIA 공식 처방이 일치**한다.

### 그래서 두 갈래의 fix

| | 무엇 | 공략하는 원인 | 비용 |
|---|---|---|---|
| **1순위** | **눈 녹여 재학습**(visual fine-tune) | 얼린 눈이 정밀 위치를 못 뽑음 | 학습(중간) |
| 차선 | **손목 카메라 추가** | 단일 카메라라 깊이가 애매 | env+데이터+학습(큼) |

NVIDIA 공식 처방이 "눈을 녹여라"이고, 이미 돌린 실험이 "head만으론 천장"임을 보였으니
**1순위 = visual fine-tune.** 단, 새로 바닥부터가 아니라 **지금 모델(v1-s20000)에서 이어서**
하는 게 정석(이미 학습된 손동작을 살리고, 거기서 눈만 추가로 푸는 2단계 레시피).

손목 카메라는, 눈을 녹여도 천장이면(=단일 시점에 정보가 정말 없으면) 그때 가는 더
근본적 카드.

---

## 5. 지금까지 결론 / 다음 수

- 색·언어: 해결(94%). 데이터·채점: 결백(검증 완료). 실행 주기: 이미 최적.
- **병목 = 얼린 눈이 큐브 3D 위치를 정밀히 못 본다.**
- **다음: v1-s20000에서 이어 visual 백본을 녹여 재학습(B).** (과금 — 시작 전 설계 합의 필요.)
- 안 되면: 손목 카메라로 관측 자체를 늘림.

---

## 곁가지 — 인프라 삽질 (CUDA 에러)

평가용 서버를 띄우다 **3대 중 2대**가 부팅 막판 `CUDA unknown error`로 죽었다(GPU는
붙어 있는데 torch가 못 잡음). 간헐적 호스트 결함으로 판단 → 서버 스크립트에 **재시도
루프**를 넣어(죽으면 새 프로세스로 다시 띄움) 이미지 재배포. 한 번에 안 뜨면 자동
재시도하도록 해결. (단, 정작 검증한 부팅은 한 번에 떠서 재시도 경로가 실제로 발동된
건 아직 못 봄 — 다음에 그 에러가 또 나야 진짜 확인됨.)

---

## 부록 A — 핵심 숫자 모음

| 항목 | 값 |
|---|---|
| 최초 성공률 | 18% (나중 재측정 32% — diffusion이라 회당 편차 큼, 참값 ~25%) |
| 색 정확도(집은 것 중 목표색) | 94% (16/17) |
| 아무 큐브나 집기 | 34% (best) |
| 실패 시 손–큐브 거리 | 평균 8.4cm (성공 시 1.1cm) |
| action_steps 16/8/4 | 성공 32% / 8% / 2% (줄일수록 악화) |
| 정답 시범 재생(채점 정직성) | 재현율 100% |
| 학습량 | s3000=0.67 epoch, s20000=4.5 epoch (둘 다 ~평탄) |

## 부록 B — 측정 방법 (어떻게 그 숫자를 뽑았나)

- **성공**: 시뮬 환경의 `evaluate()` = 목표색 큐브를 잡고 테이블 위로 들었나(z>0.06).
- **색 정확도**: 매 스텝 큐브 3개 각각 `is_grasping`+높이를 직접 읽어, 집어 든 큐브 색을
  목표색과 대조. "집은 것 중 목표색 비율".
- **채점 정직성 게이트**: `scripts/_wsl_abs_eef_replay.py` — 기록된 절대-EEF 행동을 평가와
  똑같은 디코드(rot6d→quat→IK→step)로 모델 없이 재생, 재현율 확인. (WSL, 무과금)
- **빗나감 방향**: `_wsl_gr00t_eval` 의 `miss_vec`(손−큐브 벡터). 다음 실행부터 기록됨.

## 부록 C — 한 줄 용어

- **head만 학습**: 모델의 눈·언어는 얼리고, 동작 생성부(projector+diffusion head)만 학습.
- **visual fine-tune (tune_visual)**: 얼린 눈(시각 인코더)도 같이 학습 — 인지를 바꾸는 레버.
- **action_steps**: 한 번 보고 예측한 16스텝 중 *실제로 몇 개를 실행하고 다시 볼지*(실행 노브).
- **warm-start**: 바닥부터가 아니라 이미 학습된 모델(v1-s20000)에서 이어 학습.
- **diffusion jitter**: 확산 정책이 매번 살짝 다른 계획을 뽑아, 자주 재계획하면 동작이 떨림.

## 출처 (GR00T 가이드)

- [NVIDIA/Isaac-GR00T](https://github.com/NVIDIA/Isaac-GR00T) — 기본 freeze 정책(눈·언어 frozen)
- [GR00T N1.7 단일팔 fine-tune 튜토리얼 (Seeed)](https://wiki.seeedstudio.com/fine_tune_gr00t_n1.7_for_rebot_arm_and_deploy_on_robotics_j601/)
- [Isaac GR00T 아키텍처 문서](https://nvidia-isaac-gr00t.mintlify.app/architecture)
